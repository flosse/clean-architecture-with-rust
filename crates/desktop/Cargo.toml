[package]
name = "cawr-desktop"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
# Workspace dependencies
cawr-adapter = "0.0.0"

# External dependencies
eframe = "*"
log = "*"
tokio = { version = "1", features = ["rt-multi-thread"] }
